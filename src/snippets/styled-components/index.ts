import * as Index from './Login';
import * as Form from './Form';
import * as Header from './Header';
import * as THEME from './theme';

export const StyledComponents = {
  Index,
  Form,
  Header,
  THEME
};
